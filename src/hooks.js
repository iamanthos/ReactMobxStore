// import React from 'react';
// import { store, storeContext } from './store';

// export const useStore = () => React.useContext(storeContext);

// export const useStores = () => React.useContext(storeContext)[store];
